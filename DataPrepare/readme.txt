DataPrepare

Scripts to prepare data for DeepID training, using FaceDetection and FaceAlignment.

Steps:
1) build ../FaceAlignment;
2) put webface dataset at ./webface;
3) run data_prepare.py and wait;
4) go to train ../FaceIdentification;